<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.june.northland">

    <application
        android:name=".base.app.NorthLandApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".feature.main.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".feature.character.CharacterConfigActivity" />
        <activity android:name=".feature.character.CharacterInfoActivity" />
        <activity android:name=".feature.backpack.BackpackActivity" />
        <activity android:name=".feature.lineup.LineUpActivity" />
        <activity android:name=".feature.practice.PracticeActivity" />
        <activity android:name=".feature.store.StoreActivity" />
        <activity android:name=".feature.plot.PlotActivity" />
        <activity android:name=".feature.plot.PlotBattleActivity" />
    </application>

</manifest>